# v0.9.9-rc1 — Sprint 3 Stabilization

## Baseline
- **Tag:** v0.9.9-rc1
- **Commit:** 9ba9104
- **CI:** verify ✅ integration ✅ e2e ✅
- **Blueprint:** OI-001–OI-027, G-01–G-06

## What Changed Since v0.9.8

### New Invariants
- **OI-027** — Idempotent events produce exactly one side effect (exact count assertions)
- **G-06** — Runtime Isolation Gate: DLQ empty, metrics at zero, workers active, idempotency available

### Infrastructure Fixes
| Area | Issue | Fix |
|------|-------|-----|
| CI Redis wait | Infinite loop when `redis-cli` missing | `timeout 30` + `nc -z` fallback |
| `/events/health` | Always returned `unavailable` | Inject `RedisManager` instead of unexported `REDIS_CLIENT_TOKEN` |
| Playwright | Port conflict with CI | `reuseExistingServer: true` |
| Playwright | Browser binary missing | `pnpm exec playwright install chromium` step |
| Vite | IPv6 bind (`::1`) not reachable | `host: '127.0.0.1'` |
| Vite | Proxy target `:3000` wrong | Corrected to `:4000` |
| Wait for Dashboard | Silent failure on timeout | Explicit `exit 1` after 15s |

### Test Isolation
- Sequential runtime-e2e execution (`fullyParallel: false`)
- Numeric file prefixes (`00-isolation → 05-burst-load`)
- `test.afterEach` OI-026 health assertions in all specs

## Release Criteria
- [x] CI verify  ✅
- [x] CI integration (Postgres + Redis)  ✅
- [x] CI e2e (Playwright, 10 tests)  ✅
- [x] All 27 invariants enforced
- [x] All 6 gates documented
- [x] Working tree clean
- [x] Tagged v0.9.9-rc1

## Known Debt (deferred to Sprint 4)
- 10+ flakiness validation runs pending
- Docker Compose runtime deployment model
